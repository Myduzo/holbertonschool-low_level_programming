#include "dog.h"
#include <stdio.h>
/**
 * print_dog - a function that prints a struct dog
 *
 */
void print_dog(struct dog *d)
{
if (name == NULL)
_putchar(name\: (nil));
else
_putchar(name\: name);

if (age == NULL)
_putchar(age\: (nil));

if (owner == NULL)
_putchar(owner\: (nil));

if (d == NULL)
_putchar('');
}
